default:
  suites:
    bank_ocr_features:
      paths: [ "%paths.base%/features/bankOcr" ]
      contexts:
        - BankOcrContext:
            - 4
#    basket:
#      paths: [ "%paths.base%/features/"]
#      contexts: [ FeatureContext ]

